enum ComedyFormat { standUp, improv, roast, openMic }

enum TicketTier { standard, vip, group, earlyBird }

enum ShowTime { evening, lateNight, weekend, happyHour, matinee, specialEvent }

enum AudienceType { allAges, adultOnly, couples, family }
