import 'package:flutter/material.dart';

// TODO:
class UnableToLoad extends StatelessWidget {
  const UnableToLoad({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("Unable to load page content");
  }
}
